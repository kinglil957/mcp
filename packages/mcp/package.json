{
  "name": "@watchlyhq/mcp",
  "version": "0.0.1",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run extract-metadata && tsc",
    "dev": "tsc --watch",
    "extract-metadata": "tsx scripts/extract-tools.ts",
    "lint": "ultracite format",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0"
  }
}
